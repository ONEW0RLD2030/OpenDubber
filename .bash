sudo apt install git

pip install -r requirements.txt
   sudo apt install ffmpeg          # لتشغيل FFmpeg
   git clone https://github.com/MycroftAI/mimic3-voices  # أصوات Mimic3
python src/app.py
sudo apt update && sudo apt install ffmpeg  # لنظام Linux

